# Specs index

One line per spec, oldest first. "Shipped" means the feature is live in
`main` (checked against the git log); "in progress" means a spec exists but
the code isn't merged yet.

| date | spec | status |
|---|---|---|
| 2026-09-04 | [`2026-09-04-checkpoints.md`](2026-09-04-checkpoints.md) — per-job progress ticking (windows, doors, glass, product F/S/T) | shipped |
| 2026-09-04 | [`2026-09-04-alerts.md`](2026-09-04-alerts.md) — job alerts by email, subscriptions in the dashboard, mailing by a Power Automate flow | shipped |
| 2026-09-07 | [`2026-09-07-export.md`](2026-09-07-export.md) — export jobs to Excel or PDF, filtered and field-picked, no phone/eircode ever | shipped |
| 2026-09-07 | [`2026-09-07-phases-fab.md`](2026-09-07-phases-fab.md) — the phase pipeline as primary progress, plus the first floating selection wheel (quarter-circle fan) | shipped — the wheel's look was replaced by the next spec |
| 2026-09-07 | [`2026-09-07-radial-menu.md`](2026-09-07-radial-menu.md) — the selection wheel redone as a full-circle radial menu (supersedes the fan in the previous spec) | shipped |
| 2026-09-07 | [`2026-09-07-phases-list.md`](2026-09-07-phases-list.md) — hand-set phases stored in a SharePoint list (`Dashboard phases`), never in the workbook | shipped |
| 2026-09-08 | [`2026-09-08-glass-station.md`](2026-09-08-glass-station.md) — the glass floor station: a separate tablet page fed by the master dashboard, its own SharePoint list, no workbook access | shipped 2026-09-08 — superseded by the v2 spec below |
| 2026-09-08 | [`2026-09-08-glass-station-v2.md`](2026-09-08-glass-station-v2.md) — glass station, second iteration: people and PINs on the tablet, the stages renamed to Glass cut / Hotmelt / Glazing, a `Station log` of who changed what, and ten-second delta polling on both screens | shipped 2026-09-08 — superseded by the v3 spec below |
| 2026-09-08 | [`2026-09-08-glass-station-v3.md`](2026-09-08-glass-station-v3.md) — glass station, third iteration: one row and one number per job (DG + TG, no glass types on the floor), the counters seeded from the office's own checkpoints, cards you tap directly with no bars or expanding, a search box, and a gold card when a job is finished | shipped 2026-09-08 |
| 2026-09-09 | [`2026-09-09-john-template.md`](2026-09-09-john-template.md) — selecting a whole section, the Production sheet's row colour code (urgent / booked / trade / on hold), the **John print sheet** view and export (the `Production (2)` sheet on its own terms, with phone numbers), and the print-notes window backed by the `Dashboard print notes` list. Includes the owner's second round and the review fixes in its Amendments section | shipped 2026-09-09 (bf3470b) |
| 2026-09-09 | [`2026-09-09-glass-chip-on-job-row.md`](2026-09-09-glass-chip-on-job-row.md) — the floor's glass progress on the master dashboard's own job rows: a `Glass` column showing all three stages as one number, gold when finished, repainting when the floor taps and polling fast while glass jobs are listed. No workbook write and no list write. Two review rounds in its Amendments section | shipped 2026-09-09 |
| 2026-09-09 | [`2026-09-09-tablet-glass-total.md`](2026-09-09-tablet-glass-total.md) — a running total of glass beside the tablet's search box, not narrowed by it, finished jobs excluded | shipped 2026-09-09 (`3d26677`) — superseded the same day by the spec below |
| 2026-09-09 | [`2026-09-09-tablet-my-work-left.md`](2026-09-09-tablet-my-work-left.md) — the tablet's numbers become per-person: the card and the header count down what the signed-in person has left (`total` less that stage's counter, summed over each stage they hold), so two people at one tablet see different numbers | shipped 2026-09-09 (`06dff7c`) — superseded 2026-09-10 by the per-stage spec below |
| 2026-09-10 | [`2026-09-10-glass-colours-two-way.md`](2026-09-10-glass-colours-two-way.md) — **the floor's work reaches the `Production` sheet**: the office dashboard paints DG, TG, TUFF and NOT TUFF yellow and gold from the floor's counters (fills only, fully reversible, last writer wins); a TUFF counter on the tablet with its own total; and `OfficeDone`, which makes a job read-only on the tablet. Read its Amendments section: it records one gap in the brief (`TuffTotal`) and every decision taken | shipped 2026-09-10 (`ab0c85c`) |
| 2026-09-10 | [`2026-09-10-office-clears-the-floor.md`](2026-09-10-office-clears-the-floor.md) — **an office un-tick clears the floor's counters**: clearing a job's glass in the office now puts that job's `Glass station` row's `Cut`/`Hotmelt`/`Glazed`/`Tuff` back to nought with `DoneBy`/`DoneAt`, after a confirmation naming what will be destroyed. **Changes `CLAUDE.md` rule 3** — the second and last exception to "the office never writes the floor's columns". Read its Amendments: the write is narrowed to rows the floor has really tapped, and the queued-tap decision (and the fix pass that corrected it) is recorded there | shipped 2026-09-10 (c9bbee5, then 1f2f27d). **Amendment D is the one to read**: the owner restated the rule - Excel, then the master dashboard, then the glass, and any change from the dashboard is absolute - so the colour writer now stands down for a job while an office change on it is still settling, instead of arguing about stamps. Built in the working tree, tests green, not committed. Amendment E adds the unlock (OfficeDone: No) to the clear's own write, so the tablet frees the card on its next poll instead of waiting for a feed |
| 2026-09-10 | [`2026-09-10-tablet-per-stage-counts.md`](2026-09-10-tablet-per-stage-counts.md) — the tablet counts glasses per stage and tuff on its own: one number per stage the person holds (`Cutting 20 left`, `Hotmelting 35 left`), never summed, tuff against `TuffTotal`; the headline is the job's own `DG + TG` again. Supersedes the summed per-person number | in progress |
| 2026-09-11 | [`2026-09-11-status-list-is-truth.md`](2026-09-11-status-list-is-truth.md) — **checkpoint status stops being the Excel colour**: it becomes one row per `JOB|ITEM` in the SharePoint list `Dashboard progress`, read directly by both screens and written before the colour, which becomes a copy nobody reads back. Deletes the `PENDING` hold for checkpoints, adds a one-time import of today's colours and a safeguard that adopts a cell painted in Excel by hand (confirmed through the Excel API, no waiting period). Three steps: (1) the owner makes the list, (2) checkpoints, (3) the glass colour writer | shipped 2026-09-11 (`a7aa370` + `2f28da0`, build `20260911-1642`) — steps 2 and 3 shipped together |
| 2026-09-11 | [`2026-09-11-doors-and-window-types.md`](2026-09-11-doors-and-window-types.md) — **doors become checkpoints**: the five DOORS DONE cells (BT–BX) are one door each, the cell's text its type code and its fill its status, with two stages (yellow in fabrication, gold done) ticked from the job card and recorded in `Dashboard progress` like every other checkpoint; a door coloured in Excel by hand is adopted. **Windows (M) and Doors (N) keep their own tick and also answer for what is under them** — the higher of the two is shown, and the paint only ever goes upwards, so nothing under the line ever repaints a gold M or N white. The window types move under one collapsible **Windows** heading in the card and the doors under **Doors**; the home list's WND / DRS numbers carry the aggregate colour with the breakdown in the hover, with no new column. **Changes `CLAUDE.md` rule 1** — the third and last sanctioned fill | approved 2026-09-14, built, reviewed, fix pass applied |
| 2026-09-15 | [`2026-09-15-station-comments.md`](2026-09-15-station-comments.md) — **a note channel from any floor station to the office**: a composer and this station's own thread on each job card of the tablet, one new SharePoint list (`Station comments`, append-only, `Title` a row id and not a key), and a read-only **Floor notes** section in the office drawer showing every station's notes on that job, oldest first and tagged. A new note puts one line in the Changes panel; there is no new column on the job row and nothing is written into the workbook. Built once for every station: `ST.stationComments(cfg)` takes the station's name as its only station-specific input | shipped 2026-09-16 (with amendment E: Components column blanked, unread-notes icon on the row and the board card, notes on the board cards) |
| 2026-09-21 | [`2026-09-21-glass-split-no-glazing.md`](2026-09-21-glass-split-no-glazing.md) — **glazing leaves the glass station, one tablet per stage, a new colour rule, and an editable office board**. Cutting and hotmelting are the two stages; the same `glass.html` serves both tablets, told which it is by `?stage=` / `cw_stationstage` / a two-button chooser. DG, TG and NOT TUFF are blank for neither stage complete, **yellow for exactly one**, **gold for both**; TUFF is gold on its own count. The Glass station board gains the office's own `−`/`+`/`All` per stage line and opens the job drawer from the card head — the **third sanctioned office write of a floor counter**, five fields, logged in `Dashboard Log` and never in `Station log` (dated note in `CLAUDE.md` rule 3). **No existing data is rewritten, reset or deleted**: `Glazed`/`GlazedBy`/`GlazedAt` stay on the list, read and written by nothing, and old `glazed` log lines still show, labelled "Glazing" | shipped 2026-09-21, build 20260921-1858 |
| 2026-09-21 | [`2026-09-21-day-sheets-and-station-reports.md`](2026-09-21-day-sheets-and-station-reports.md) — **the cutter's end-of-day sheet, a weekly target, and one export template for every station**. Two new lists (`Station day sheets`, append-only from the tablet, one row per person per station-stage per day; `Station targets`, the office's alone) in whichever site the station's lists live. Switched on by the station **definition** (`daySheets`), so hotmelting or welding get one by gaining a line and a stage without one draws no button and reads neither list. The office reads them by date, weekday and ISO week against the target, may correct a saved sheet (counts and note only) and is the only writer of the target — the **new dated exception in `CLAUDE.md` rule 2**, both logged in `Dashboard Log`, neither in `Station log`, nothing deleted, nothing near the workbook. **Station report**: one pure `stationReport(def, stage, data, period)` with no station-specific branch — the Jobs sheet comes from each definition's own `reportJobs` adapter — producing Summary / Days / Jobs / Activity / Notes, with rule 3's strip (now `ST.stripContact`, shared with welding) over every piece of free text | shipped 2026-09-21, build 20260921-1858 |
| 2026-09-21 | [`2026-09-21-glazing-station.md`](2026-09-21-glazing-station.md) — **the third floor station, and the phase bar hearing the floor**. A tablet page for the glazer (`glazing.html`), an office board at Show ▸ Glazing station, and one row per **job** in a new `Glazing station` list in the `Floor stations` site. **One number per job**: units glazed out of the job's windows plus doors, read off the `Production` sheet alone (`j.wndMain`/`j.drsMain`, the new Production-only pair beside `j.prodsMain`). No seed — glazing was never an office checkpoint, so `seedFields` is empty and the feeder's whole vocabulary holds not one floor column. No day sheet. The office may set and clear the counter from its board — five fields, logged in `Dashboard Log`, never in `Station log` (dated note in `CLAUDE.md` rule 3). **Nothing writes the workbook.** Section E adds a **third voice to `effectivePhase`**: welding recorded → at least In fabrication, glazing recorded → at least In glazing, glazing outranks welding, the highest of sheet / hand-set / floor wins, a counter back at nought withdraws the voice, and **none of it is stored anywhere**. Built from the "Adding a station" checklist rather than by copying a page, which is the evidence the checklist works. **Section F (glazing complete → Ready to deliver) is NOT built** — it is a row move on the `Production` sheet and waits on the owner's choice | sections A–E shipped 2026-09-21, build 20260921-1858; section F not built |
| 2026-09-16 | [`2026-09-16-welding-station.md`](2026-09-16-welding-station.md) — **the second floor station**: a tablet page for the PVC welders (`welding.html`), an office board at Show ▸ Welding station, and one row per job **and product group** in a new `Welding station` list in the `Floor stations` site. F and S only (never T), every product group on the sheet minus a four-name deny-list, the COMMENT carried with a rule-3 strip over it (6+ digits and anything eircode-shaped removed), a three-level colour (none / yellow / green), and the office able to set and clear progress per group — the **second sanctioned office write of a floor counter**, logged in `Dashboard Log` and never in `Station log` (dated note in `CLAUDE.md` rule 2). **Nothing writes the workbook.** Also the pattern for the third station: a station **definition** object, `welding-core.js`, the shared `station-ui.js` shell, and the "Adding a station" checklist in `docs/STATIONS.md` | built 2026-09-16, reviewed and demoed before any push |
| 2026-09-22 | [`2026-09-22-glazing-board-polish.md`](2026-09-22-glazing-board-polish.md) — **the Glazing station board, office side only**: its own CSS grid tracks so the row lines up instead of inheriting welding's; gold shown when the job's whole `Production` row is already gold or its glazed count is full (`GLZC.glzOfficeColour`), with the steppers replaced by the word "done" on a gold row; the progress bar removed, the count and quantity words kept. Display only — no list write, no workbook write, no seed. Review found the steppers track had to be a fixed 170px, not `auto` | shipped 2026-09-22, build 20260922-1309 |
| 2026-09-23 | [`2026-09-23-welding-remake-counter.md`](2026-09-23-welding-remake-counter.md) — **a remake counter beside All on the welding tablet**: `FramesRemade` / `SashesRemade` list columns (owner-created), counter keys `frames-remake` / `sashes-remake` through the same queue as a done tap, count-only PATCH, no ceiling, no effect on progress or the report's units; office reads it on the board line, the drawer and the report's Jobs sheet, never writes it | shipped 2026-09-23, build 20260923-1115 |
| 2026-09-23 | [`2026-09-23-glazing-windows-only.md`](2026-09-23-glazing-windows-only.md) — **the glazing unit is a window, not windows plus doors**: `glzSlice`'s `total = wnd` alone, so a job with 11 windows and 2 doors glazes to 11; a door-only job leaves the glazing station entirely (`Active = No` on the feeder's next pass); card, board and report words say windows only, `Doors` still fed as a fact but not counted. No workbook write, no list column change | shipped 2026-09-23, build 20260923-0942 |
| 2026-09-24 | [`2026-09-24-glazing-astragal.md`](2026-09-24-glazing-astragal.md) — **astragal is the glazing station's second count**: `AstragalTotal` from the `Production` sheet's ASTRAGAL, `Astragal` done; its own line with − + All on the tablet and the office board; a job with windows or astragal is fed; done only when both are full. Two new Number columns added by script. No workbook write | built, awaiting go |
