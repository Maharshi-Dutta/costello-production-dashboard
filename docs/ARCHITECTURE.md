# Architecture

This used to be one 364-line file. As of 2026-09-22 it is an index into
linked topic notes under `web/docs/`, cross-referenced with Obsidian-style
`[[wikilink]]` syntax. Every sentence that used to live here moved verbatim
into one of the notes below — nothing was summarised or dropped.

Last updated 2026-09-22 (split into linked notes; content unchanged).

## Reading order

- **Data flow, in text; the 36-second file lag and `PENDING` holds** —
  [[data-flow-and-lag]]
- **Dashboard-owned sheets; SharePoint lists and the scope split** —
  [[sheets-and-lists-scope]]
- **The station feeder and the station page; the welding station** —
  [[station-feeder]]
- **Module map; key invariants; where state lives** —
  [[module-map-and-invariants]]

## See also

- [[REFERENCE]] — the companion index: what has been built, and how
- [[overview-and-process]] — the shape of the whole system these notes describe
